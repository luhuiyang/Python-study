# 直接从笔记里面选一些来吧
#
# 写程序要注意一致性
# 应该把功能封装生一个个函数，需要的时候就直接调用，而不是每次都去实现一次
# 要多想how(怎么实现一个功能)，而不要过多考虑what(具体的实现)
#
# 类用class定义 class 类名(父类)，类名取名用驼峰命名法
# 用__init__(self)设置类的初始属性
# 用变量名 = 类名()
# 初始化一个类的实例类的函数叫方法，每个方法的第一个参数是self，调用的时候不需要传
# 带__的函数叫魔法函数，大部分魔法函数用来转换运算符号
# 可以定义类的魔法函数，来实现用运算符号操作类：
#
# 用if的时候，类型要用显示的，不要用隐式的类型
#
# 写代码是，要注意抽取重复的东西，简化代码，把功能独立出来，拆分逻辑
# 要增强一致性，消除特例，如果没有一致性，就凑出一致性
# 二进制用0b1, 0b10，0o1, 0o7, 0xff, 0x10, 0b开头表示2进制，0o开头表示8进制， 0x开头表示16进制
# 变量名取名用小写字母 + 下划线的方式，临时变量可以用单个字母。
#
#
#
